{
  "name": "@gitterm/db",
  "type": "module",
  "exports": {
    ".": {
      "default": "./src/index.ts"
    },
    "./*": {
      "default": "./src/*.ts"
    }
  },
  "scripts": {
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:seed": "bun run ./src/seed-cli.ts"
  },
  "dependencies": {
    "@gitterm/schema": "workspace:*",
    "dotenv": "catalog:",
    "drizzle-orm": "catalog:",
    "pg": "^8.14.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@gitterm/config": "workspace:*",
    "@types/pg": "^8.11.11",
    "drizzle-kit": "catalog:"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}

{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "bun build filter=web"
      },
    "deploy": {
      "startCommand": "cd apps/web && bun run dist/index.mjs",
      "healthcheckPath": "/",
      "healthcheckTimeout": 100,
      "restartPolicyType": "never"
      }
  }
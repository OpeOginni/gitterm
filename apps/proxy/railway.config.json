{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "bun build filter=proxy"
      },
    "deploy": {
      "startCommand": "cd apps/proxy && bun run dist/index.mjs",
      "healthcheckPath": "/",
      "healthcheckTimeout": 100,
      "restartPolicyType": "never"
      }
  }
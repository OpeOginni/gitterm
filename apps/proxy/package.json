{
  "name": "proxy",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "caddy run --config Caddyfile.local --envfile .env --watch"
  },
  "dependencies": {
    "@gitterm/config": "*"
  },
  "devDependencies": {
    "@gitterm/config": "*"
  }
}
PORT=9000
REDIS_URL=redis://localhost:6379
INSTANCE_ID=local-dev
TUNNEL_JWT_SECRET=
INTERNAL_API_KEY=
SERVER_URL=

# =============================================================================
# ROUTING MODE CONFIGURATION
# =============================================================================
#
# For path-based routing (local development):
# ROUTING_MODE=path
#
# For subdomain-based routing (production):
# ROUTING_MODE=subdomain
# =============================================================================

# Routing mode: "path" (local dev) or "subdomain" (production)
ROUTING_MODE=path
